'use strict';

var React = require('react-native');
var {
  Text,
} = React;

var Login = React.createClass({
    render: function () {
        return (
            <Text>Hello</Text>
        );
    }
});

module.exports = Login;
